export function NavBar() {
    return (
        <header className="sticky top-0 z-10 bg-sky-500 text-white">
            <nav className="container mx-auto px-4 py-4 flex items-center">  {/* Wrap in nav tag */}
                <a href="#" className="font-bold text-xl">Carry1st</a>
            </nav>
        </header>
    );
}